# 🧠 PII Analyzer API

A FastAPI-based service to **extract text from documents (OCR)** and **detect PII entities** using **Microsoft Presidio**.

---

## 🚀 Features
- Upload files (`.png`, `.jpg`, `.jpeg`, `.txt`)
- OCR text extraction using **Tesseract**
- PII detection using **Presidio Analyzer**
- REST API built with **FastAPI**

---
